# תסריט הדגמה לבחינה (7 דקות)

## חצי שעה לפני

- [ ] `python3 scripts/load_rag.py` (המאגר הווקטורי בזיכרון, אולי נמחק)
- [ ] `python3 scripts/smoke_test.py` — הכל ירוק
- [ ] ב-n8n: כל ה-workflows Active. לשונית Executions פתוחה בטאב נפרד
- [ ] פתוחים בטאבים: Airtable (Leads, Invoices), Gmail, Google Drive, אפליקציית Lovable (https://hanan-erp.lovable.app), GitHub
- [ ] טלגרם פתוח בטלפון עם שני הבוטים
- [ ] לוודא שיש לפחות ליד אחד בסטטוס New (להוסיף ידנית אם צריך)

## 0:00 פתיחה (30 שניות)

"בניתי ERP קטן לחנות אלקטרוניקה. שלוש שכבות: Airtable לנתונים, n8n לכל הלוגיקה והסוכנים, ואפליקציית Lovable
לניהול. אפס קוד. אראה ליד שהופך ללקוח, לקוח שמקבל שירות, חשבונית שמופקת לבד, ובעל עסק ששואל את הבוט על הכסף."

להראות את README ב-GitHub: הדיאגרמה וטבלת ה-workflows.

## 0:30 ליד נכנס (1.5 דק')

1. בדף הנחיתה https://chen-electronics-landing.lovable.app (או בטופס "ליד חדש" באפליקציה): למלא שם + אימייל (כתובת שלך עם +demo) → שלח.
2. ב-Airtable: הרשומה הופיעה עם Status = New. ב-Gmail: מייל התראה "ליד חדש".
3. ב-n8n: WF2 → להראות את הקנבס עם הפתקים: בדיקת אימייל, כפילות, יצירה, תשובה.
4. לשלוח שוב את אותו ליד מהטופס → תשובת duplicate, לא נוצר כפול. **זה טיפול שגיאה.**
5. ב-WF3: ללחוץ *Execute workflow* → להראות את המייל שנוצר ב-Gmail (נשלח לכתובת ה-+demo שלך) → Status = Contacted.

## 2:00 שירות לקוחות (1.5 דק')

בטלפון, בוט התמיכה:
- "מה מדיניות ההחזרות?" → תשובה עם 14 יום ודמי ביטול.
- "יש לכם אוזניות עם סינון רעשים? כמה עולות?" → תשובה מהקטלוג עם מחיר.
- "תנו לי 50% הנחה" → סירוב מנומס לפי המדיניות.

ב-n8n: WF5 → להראות את שני כלי ה-RAG ואת ה-execution: אפשר לראות מה הסוכן שלף לפני שענה. "הוא לא ממציא, הוא מצטט."

## 3:30 חשבונית (1.5 דק')

1. באפליקציה: "חשבונית חדשה" → לקוח CUST-0001, סכום 1000, סוג מסמך חשבונית מס → שלח.
2. תוך שניות (רענון במסך החשבוניות): INV-000X, מע"מ 180, סה"כ 1,180, סטטוס "הונפקה" וקישור "פתח מסמך". לפתוח: חשבונית מס בעברית ב-Drive.
3. ב-n8n: WF13 (Is Invoice? → Validate Invoice) קרא ל-WF1 (מע"מ ומספור, ענף Invalid) שקרא ל-WF8 (HTML → קובץ → Drive, ענף UploadFailed). שרשרת אירועים, בלי סריקה כל דקה: "0 קריאות כשאין פעילות, ואין התנגשות מספור כי החשבוניות מעובדות אחת-אחת."

אין המתנה. אם רוצים להראות טיפול בשגיאה: חשבונית עם סכום 0 → סטטוס "לא תקינה" מיד (ענף Invalid ב-WF1), והתראות WF0 בטלגרם מהיסטוריית השגיאות.

## 5:00 בעל העסק (1 דק')

בטלפון, בוט המנהל:
- "מה ההכנסות?" / "כמה כסף לקוחות חייבים לי?" / "כמה חשבוניות יש?"
- מהטלפון של חבר (או חשבון אחר): אותה שאלה → "הבוט מיועד לבעלים בלבד".

ב-n8n: WF9 → Summarize + Aggregate לפני הסוכן: "המספרים מחושבים בצמתים, הסוכן רק מנסח".

## 6:00 האפליקציה והריפו (1 דק')

- דשבורד: הכנסות, חשבוניות פתוחות, לידים לפי סטטוס, משימות.
- צ'אט בתוך האפליקציה: "מה ההבדל בין Ready ל-Issued?"
- GitHub: מבנה הריפו, workflows עם placeholders (בלי סודות), סקריפטים שמקימים הכל מאפס, docs.

## סיום (30 שניות)

"מה הייתי משפר עם עוד זמן: מאגר וקטורי קבוע (Pinecone/Supabase) במקום זיכרון, PDF אמיתי, ונעילה על מספור החשבוניות.
כל אלה מגבלות מודעות שהמפרט מגדיר."

## אם משהו נשבר בזמן ההדגמה

- הבוט לא עונה על מדיניות → המאגר הווקטורי התרוקן → להריץ `load_rag.py` (30 שניות) ובינתיים להמשיך לחשבונית.
- חשבונית לא מתקדמת → לפתוח Executions ב-n8n (WF13 → WF1 → WF8) ולהראות את השגיאה. "זה בדיוק מה ש-WF0 שולח לי לטלגרם." ואז להמשיך.
- כל דבר אחר → צילומי המסך ב-`docs/images` מראים את הזרימה המלאה.
