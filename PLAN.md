# תוכנית עבודה — פרויקט גמר AI-ERP with n8n

מטרה: ריפו מסודר ב-GitHub + מערכת חיה שאפשר להדגים בבחינה. רמת דמו, לא מוצר.
עיקרון: הדרך הקצרה ביותר. משתמשים בתבניות ה-workflows של הקורס, מתקנים, מוסיפים מה שהקורס דורש, ומתעדים יפה.

---

## 1. מה למדתי מהדוגמה (etet16ai-jpg/AI-PMO)

**מה יש בריפו שלו**
- `workflows/` — 10 קבצי JSON (WF 1,2,3,4,5,6,7,8,9,13). 
- `README.md` — טבלת workflows, "סיפור" עם 14 צילומי מסך (ליד נכנס → מייל קר → תשובה → חשבונית → בוט שירות → בוט מנהל → דשבורד), מודל נתונים, שירותים חיצוניים, מגבלות ידועות.
- `docs/images/` — צילומי מסך של כל workflow, של טלגרם, של Airtable ושל האפליקציה.
- `final-project-spec.docx` — דף הדרישות של הקורס (אותו מסמך שיש לנו).
- `docker-compose.yml` + `Caddyfile` — n8n עצמאי בשרת שלו. **לא רלוונטי לנו**, נעבוד על n8n Cloud כמו שהקורס ממליץ.

**ממצא חשוב:** ארבעת הקבצים שיש לך ב-Downloads (Leads, Cold Emails, Policies, Products) זהים אחד לאחד ל-WF2/3/6/7 שלו, כולל אותו webhook path ואותם באגים. כלומר אלה תבניות שהקורס סיפק, והוא לא כתב אותן. ששת ה-workflows הנוספים אצלו (1, 4, 5, 8, 9, 13) בנויים באותו סגנון ובאותם צמתים שהמדריך מתאר. נשתמש בהם כבסיס לשלנו.

**מה הוא עשה טוב (נעתיק)**
- פתק דביק (Sticky Note) בעברית על כל צומת + פתק כותרת לכל workflow. זה מה שהבוחן רואה כשפותחים את הקנבס.
- בוט מנהל חסום ל-Chat ID של הבעלים בלבד (IF לפני הסוכן). המדריך דורש את זה במפורש.
- סוכן שירות לקוחות עם שני כלי vector store (מדיניות + מוצרים) ו-System Message שאוסר להמציא.
- WF13: webhook אחד עם Switch לפי `body.action` (read / create / update / chat). האפליקציה מדברת רק איתו.
- README שמספר סיפור עסקי עם תמונות, לא רשימת צמתים.

**מה הוא לא עשה (ההזדמנות שלנו להיות טובים יותר בקלות)**
- **אין טיפול בשגיאות בכלל**, והמדריך דורש "לפחות טיפול בשגיאה אחת בכל אוטומציה". נוסיף.
- השאיר באג: שליחת מייל קר ל-`Emailsds` במקום `Email`. נתקן.
- WF2 שולח מייל התראה עם נושא "asdasd". נכתוב טקסט אמיתי.
- אין נתוני דמו. נזרע נתונים כדי שהדשבורד והמנהל יראו משהו.
- אין תסריט הדגמה. נכין אחד.

---

## 2. החלטות (סוכמו 16.9.2026)

| נושא | החלטה | למה |
|---|---|---|
| שם העסק | **Chen Electronics / חן אלקטרוניקה** | החלטת הסטודנט. המדיניות, הפרומפטים ותבנית החשבונית מותגו מחדש. |
| n8n | **n8n Cloud** (trial 14 יום או תוכנית Starter) | אפס התקנה. דדליין: 30.9.2026 (שבועיים מ-16.9). ה-trial מכסה בדיוק את התקופה; אם הבחינה אחרי 30.9 צריך Starter. |
| אפליקציה | **Lovable** | החלטת הסטודנט. הפרומפט המלא ב-`app/lovable-prompt.md`. האפליקציה קוראת ישירות ל-webhook (CORS פתוח), בלי backend משלה. |
| מודל | gpt-4o-mini לסוכנים, text-embedding-3-small ל-RAG | זול, מספיק לדמו, תומך עברית. |
| Gmail | חשבון Gmail ייעודי לדמו (מומלץ) | שלא ישלחו מיילים קרים מהחשבון האישי שלך בטעות. |
| שפה | UI ומיילים בעברית, README באנגלית עם עברית | כמו הדוגמה. |

---

## 3. חלוקת עבודה: מה אני עושה לבד, מה אתה

### שלב 0 — חשבונות ומפתחות (אתה, ~45 דק', חד-פעמי)

זה השלב היחיד שבאמת דורש אותך, כי מדובר בכניסות לחשבונות ובסודות שאסור לי להזין.

1. **OpenAI** — יש לך כבר מפתח משיעור 28. ודא שיש יתרה (5$ מספיק לכל הפרויקט).
2. **n8n Cloud** — פתח חשבון ב-n8n.io. אחר כך: Settings → n8n API → Create API key. תן לי את **כתובת ה-instance** (למשל `https://xxx.app.n8n.cloud`) ואת **ה-API key**. איתם אני מייבא ומפעיל את כל ה-workflows בלי שתגרור צומת אחד.
3. **Airtable** — פתח base ריק בשם `AI-ERP`. צור Personal Access Token עם הרשאות `data.records:read/write` ו-`schema.bases:read/write` על ה-base הזה. תן לי **PAT + Base ID** (`app...` מה-URL). איתם אני יוצר את 4 הטבלאות עם השדות המדויקים וזורע נתוני דמו.
4. **Telegram** — ב-@BotFather צור שני בוטים: `<שם>_manager_bot` ו-`<שם>_support_bot`. שלח הודעה לבוט המנהל, ואז תן לי את **ה-Chat ID שלך** (פתח `https://api.telegram.org/bot<TOKEN>/getUpdates` בדפדפן וחפש `"chat":{"id":...}`). את ה-tokens עצמם **אל תיתן לי**, הכנס אותם ישירות ל-n8n בשלב הבא.
5. **Google** — Gmail + Drive. ב-n8n Cloud זה כפתור "Sign in with Google", בלי Google Cloud Console.
6. **GitHub** — ודא שיש לך חשבון. נתקין `gh` ותתחבר פעם אחת מהטרמינל (`gh auth login`). מרגע זה אני יוצר את הריפו ודוחף לבד.
7. **Lovable** — פתח חשבון ב-lovable.dev. נצטרך אותו רק בשלב 3.

**ב-n8n: צור 6 credentials** (Credentials → Add): OpenAI, Airtable (Token), Telegram (manager), Telegram (support), Gmail (OAuth), Google Drive (OAuth). לכל credential, פתח אותו והעתק לי את ה-**ID מה-URL** (`.../credentials/<ID>`). עם 6 המזהים אני מזריק אותם לכל הצמתים לפני הייבוא, ולא תצטרך לבחור credential ידנית ב-40 צמתים.

### שלב 1 — הריפו והקבצים (אני, ללא תלות בך)

אפשר להתחיל מיד, במקביל לשלב 0.

- `git init`, `.gitignore` (`.env`, `*.local.*`), `.gitattributes`.
- `workflows/` — 10 קבצי JSON נקיים על בסיס תבניות הקורס:
  - תיקון `Emailsds` → `Email`.
  - WF2: מייל התראה אמיתי לבעלים ("ליד חדש: שם / אימייל / טלפון").
  - איחוד כל צמתי Airtable ל-credential אחד ולשם base אחיד.
  - **טיפול בשגיאות (דרישת הקורס):** workflow מספר 0 `Error Handler` עם Error Trigger שמשלח הודעת טלגרם לבעלים עם שם ה-workflow והשגיאה. כל 10 ה-workflows מוגדרים אליו ב-Settings → Error Workflow. בנוסף ב-WF1 ענף Invalid (כבר קיים), וב-WF3 `Continue on error` על Gmail כדי שליד אחד שגוי לא יפיל את כל הריצה.
  - פתקים דביקים בעברית על כל צומת (יש בסיס בקבצי הדוגמה, נערוך לשלנו).
  - WF9: ה-Chat ID של הבעלים כפרמטר אחד בצומת IF.
- `data/products.csv`, `policies/` (12 קבצי המדיניות שכבר יש).
- `scripts/` (Python, ללא תלויות מיוחדות):
  - `airtable_setup.py` — יוצר את Invoices / Leads / Products / Tasks עם השדות הנכונים (Created כ-Created time, Status כטקסט), וזורע: 34 מוצרים מה-CSV, 5 לידים, 6 חשבוניות במגוון סטטוסים, 4 משימות.
  - `n8n_import.py` — מזריק credential IDs, מייבא את 11 ה-workflows דרך ה-API, מגדיר error workflow, מפעיל את מה שצריך להיות פעיל.
  - `load_rag.py` — שולח את policies ו-products.csv לטפסים של WF6/WF7 (מילוי המאגר הווקטורי) בפקודה אחת. שימושי כי המאגר נמחק בכל restart.
  - `smoke_test.py` — יורה ליד לדוגמה ל-WF2, יוצר חשבונית ב-Airtable, ובודק דרך ה-API של n8n שהריצות ירוקות ושהקישור ל-Drive נכתב.
- `docs/` — ארכיטקטורה (דיאגרמה), סכימת Airtable, כללי מע"מ, **תסריט הדגמה לבחינה**, שאלות צפויות ותשובות.
- `README.md` — במבנה של הדוגמה: 3 שכבות, טבלת workflows, walkthrough עם תמונות, מגבלות.
- `app/lovable-prompt.md` — פרומפט הבנייה ל-Lovable מוכן להדבקה, כולל מבנה ה-JSON של WF13.

### שלב 2 — חיבור והפעלה (אני, אחרי שתמסור מפתחות, ~שעה)

- מריץ `airtable_setup.py` → טבלאות + נתונים.
- מריץ `n8n_import.py` → 11 workflows בענן, מחוברים, פעילים.
- מריץ `load_rag.py` → המאגר הווקטורי מלא.
- מריץ `smoke_test.py` → ליד → מייל קר → חשבונית → HTML בדרייב. מתקן מה שנשבר.
- **ממך כאן (5 דק'):** לשלוח לבוט התמיכה "מה מדיניות ההחזרות?" ו"יש לכם אוזניות עם ANC?", ולבוט המנהל "מה ההכנסות?" ו"כמה חשבוניות לא שולמו?". אני קורא את ה-executions ומתקן פרומפטים אם צריך.

### שלב 3 — האפליקציה ב-Lovable (אתה עם פרומפט שלי, ~30 דק')

- אני נותן לך: פרומפט מלא + כתובת ה-webhook של WF13 + הנחיה איפה לשים אותה כ-secret.
- אתה מדביק, לוחץ Build, ואז 3–4 שיפורים קטנים בצ'אט ("סנן לפי סטטוס", "הוסף גרף הכנסות").
- אם תתחבר ל-Lovable בדפדפן המובנה של Claude, אני יכול לעשות את זה במקומך.

### שלב 4 — תיעוד, צילומי מסך, GitHub (משותף, ~שעה)

- **אתה:** צילומי מסך של שיחות הטלגרם (2), של האפליקציה (2), ושל טבלת Leads ב-Airtable (1). זרוק לי לתיקייה.
- **אני:** צילומי מסך של כל workflow ב-n8n (דרך הדפדפן המובנה אחרי שתתחבר), של ההודעות ב-Gmail, של המסמך בדרייב. כותב README סופי, docs, תסריט הדגמה. commit + push ל-GitHub.

### שלב 5 — חזרה לבחינה (אתה, ~30 דק')

- עוברים יחד על תסריט ההדגמה (7 דקות): ליד חדש מהאתר → Airtable → מייל קר → לקוח שואל בטלגרם → הזמנה נסגרת → חשבונית נוצרת אוטומטית עם מע"מ ומספר עוקב → מסמך בדרייב → המנהל שואל בטלגרם על הכנסות → הדשבורד.
- רשימת 10 שאלות צפויות ("למה in-memory?", "מה קורה אם שתי חשבוניות באותה דקה?", "איפה ה-PAT של Airtable?", "מה זה RAG ולמה לא רק פרומפט?") עם תשובות קצרות.
- **לפני הבחינה:** להריץ `load_rag.py` (המאגר הווקטורי נמחק ב-restart) ו-`smoke_test.py` כדי לוודא שהכל ירוק.

---

## 4. מבנה הריפו הסופי

```
ai-erp-n8n/
├── README.md
├── PLAN.md                     (הקובץ הזה)
├── CLAUDE.md
├── .gitignore  .env.example
├── workflows/
│   ├── 00-error-handler.json
│   ├── 01-tax-validation.json
│   ├── 02-leads-intake.json
│   ├── 03-sales-cold-emails.json
│   ├── 04-sales-reply-check.json
│   ├── 05-customer-service-agent.json
│   ├── 06-policies-to-vector-store.json
│   ├── 07-products-to-vector-store.json
│   ├── 08-invoice-maker.json
│   ├── 09-manager-agent.json
│   └── 13-app-api.json
├── data/products.csv  data/seed/*.json
├── policies/*.md
├── scripts/  airtable_setup.py  n8n_import.py  load_rag.py  smoke_test.py
├── app/lovable-prompt.md
└── docs/  architecture.md  airtable-schema.md  vat-rules.md  demo-script.md  exam-qa.md  images/
```

---

## 5. סדר פעולות מומלץ (ציר זמן)

| מתי | מי | מה |
|---|---|---|
| עכשיו | אני | שלב 1 במלואו: ריפו, 11 workflows מתוקנים, סקריפטים, docs ראשוניים |
| במקביל | אתה | שלב 0: חשבונות, credentials ב-n8n, למסור לי: n8n URL + API key, Airtable PAT + Base ID, Chat ID, 6 credential IDs |
| אחרי מסירה | אני | שלב 2: הקמה, ייבוא, RAG, בדיקות |
| אח"כ | אתה | 4 הודעות לבוטים, פרומפט ל-Lovable |
| אח"כ | שנינו | צילומי מסך, README, push ל-GitHub |
| לפני הבחינה | אתה | חזרה על תסריט, `load_rag.py`, `smoke_test.py` |

---

## 6. מה שאני צריך ממך כדי להתחיל את שלב 2

שים בקובץ `.env` בתיקיית הפרויקט (הקובץ ב-`.gitignore`, לא עולה לגיט):

```
N8N_URL=https://xxx.app.n8n.cloud
N8N_API_KEY=...
AIRTABLE_PAT=...
AIRTABLE_BASE_ID=app...
OWNER_TELEGRAM_CHAT_ID=...
CRED_OPENAI=...
CRED_AIRTABLE=...
CRED_TELEGRAM_MANAGER=...
CRED_TELEGRAM_SUPPORT=...
CRED_GMAIL=...
CRED_GDRIVE=...
```

בנוסף OWNER_EMAIL (כתובת ה-Gmail שחיברת ל-n8n).

---

## 7. סטטוס

- [x] שלב 1 הושלם (16.9.2026): ריפו, 11 workflows, סקריפטים, docs, README, פרומפט Lovable, מיתוג Chen Electronics.
- [x] שלב 0 הושלם (18.9.2026): חשבונות, 6 credentials, .env.
- [x] שלב 2 הושלם (18.9.2026): Airtable עם נתוני דמו, 11 workflows ב-n8n (מודבקים דרך הדפדפן, ה-API חסום ב-trial), Error Workflow על כולם, RAG נטען, smoke test 10/10.
- [x] שלב 2ב הושלם (18.9.2026): שני הבוטים נבדקו ועונים נכון (RAG, סירוב הנחה, מחזור 38,832 ₪, חוב 23,800 ₪). תוקן עיצוב הדגשה בטלגרם.
- [x] שלב 3 הושלם (18.9.2026): האפליקציה נבנתה ב-Lovable מהפרומפט (פרויקט "Chen Electronics Hub"). נבדק מול n8n: דשבורד עם המספרים הנכונים, לידים/חשבוניות/מוצרים/משימות, סימון משימה (update), צ'אט (WF13). פורסם ב-https://hanan-erp.lovable.app (19.9.2026). **נותר:** 5 השיפורים הקטנים מ-app/lovable-prompt.md — אופציונלי, כי קרדיטי הבנייה החינמיים של היום נגמרו (מתאפסים ב-03:00 שעון ישראל).
- [~] שלב 4 (19.9.2026): צילומי מסך ב-docs/images (6 מסכי אפליקציה, 3 שיחות טלגרם, 11 קנבסים של n8n) + סעיף Screenshots ב-README. **נותר:** brew install gh + gh auth login (הסטודנט), יצירת ריפו ו-push. **ידוע:** בקנבסים של WF0/WF5/WF9/WF13 פתקי ההסבר מסתירים חלקית צמתים ואת פתק הכותרת (מיקום הפתקים בגנרטור); אופציונלי לתקן ולהדביק מחדש לפני הבחינה.
- [ ] **חסימה (19.9.2026 08:38):** מכסת ה-trial של n8n Cloud נגמרה (1000/1000 executions) וההרצות נעצרו. הגורמים: WF8 כל דקה (~1,440 ביום) + רענון הדשבורד באפליקציה כל 60 שניות (3 קריאות ל-WF13 בכל רענון). בנוסף credential ה-Gmail דורש Reconnect (שגיאות WF4 מ-22:30). החלטה נדרשת: n8n מקומי בחינם (npx n8n + tunnel, ייבוא אוטומטי בסקריפט, הסטודנט מזין מחדש credentials) או שדרוג Cloud בתשלום. **הוחלט (20.9.2026): מעבר ל-n8n עצמי של הסטודנט (שרת פרטי, ללא מכסת הרצות; הכתובת רק ב-.env).** 6 credentials נוצרו שם מחדש, הייבוא רץ דרך ה-API. שינויים: חשבוניות בשרשרת אירועים WF13 → WF1 → WF8 (Execute Workflow, בלי סריקה של Airtable, 0 קריאות במנוחה), WF4 כל 3 שעות, Set v3.4 (השרת לא מכיר v3.5), רענון אוטומטי בדשבורד בוטל. כלל מחייב: לא להתקרב ל-rate limits של אף ספק (הסקריפטים ממתינים בין קריאות). מצב 20.9: 11 workflows מפורסמים בשרת החדש, טפסי RAG פעילים, כתובת ה-webhook באפליקציה עודכנה. credential Airtable PAT הוחלף וכל 11 יובאו שוב. פערי מפרט נסגרו (20.9): טבלאות Customers/Orders + מסכים באפליקציה, DocType (חשבונית מס/קבלה/חשבונית) בטבלה, ב-WF1, ב-WF8 ובטופס, ענף שגיאה מקומי ב-WF8 (UploadFailed), גרף לידים לפי סטטוס. RAG נטען ו-smoke test עבר 10/11 (הכישלון: הרצות ישנות מלפני תיקון ה-credential); הבוטים אושרו על ידי הסטודנט; צילומי האפליקציה עודכנו. צילומי הקנבס של WF1/WF8/WF13 והדשבורד התקבלו מהסטודנט (20.9). GitHub: https://github.com/Chenm-tech/chen-erp-n8n (ציבורי, 20.9.2026; ההיסטוריה נדחסה ל-commit אחד כדי שלא יישאר שם שרת). מופע n8n Cloud הישן נמחק על ידי הסטודנט (20.9). הפרויקט מוגש ומושלם. דף הנחיתה הושלם (21.9.2026): פרויקט Lovable נפרד "Chen Electronics Demo" מהפרומפט ב-`app/landing-prompt.md`, פורסם ב-https://chen-electronics-landing.lovable.app, טופס בדיקה אחד עבר ל-WF2 (ליד New ב-Airtable, הרצה success). תיקוני טקסט לפי הסטודנט: בלי "מחיר סופי" בטופס, משלוח 1–5 ימי עסקים לפי המדיניות, בלי שורת כתובת בפוטר, כרטיס טלגרם מציין סוכן AI עם קישור לבוט התמיכה. צילום ב-docs/images/app-landing.png, כתובת ב-README. **אין משימות פתוחות.** כיבוי אחרי קבלת התעודה (25.9.2026): 11 workflows כובו, כל ה-credentials ומפתח ה-API נמחקו מ-n8n; OpenAI/Google Cloud/Airtable/Telegram נסגרו, פרויקטי Lovable נמחקו, הריפו נשאר ציבורי ומאורכב. חזרה להצגה: רק אם הסטודנט יבקש (המרצה לא דרש).
- [ ] שלב 5 — חזרה לבחינה.
